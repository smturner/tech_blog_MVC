<h1>this is the dashboard</h1>
{{!-- {{if posts.length}}
<section>
    <h2> Your Posts</h2>
    {{#each posts as |post|}}
    {{>dashboard-postinfo post}}
    {{/each}}
</section>
{{/if}} --}}
<button type="submit" class="btn btn-dark btn-lg"> <a class="link" href="#">+ New Blog Post</a></button>

<script src="js/addPost.js"></script>